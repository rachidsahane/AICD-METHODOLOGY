# Build output. The PDF is a build artifact of src/, never a source file.
dist/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Operating system cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# The PDF the author added by hand before the build existed. Kept out of
# the repository so it is never committed by accident; it holds the only
# copy of the original cover art and author photograph.
/AICD_Methodology_v0.3.pdf
